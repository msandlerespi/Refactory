Drop your notification sound here as:

    notify.mp3

That exact filename and location (public/sounds/notify.mp3) is what the app
loads. Keep it short — 1 to 3 seconds works best.

Until the file exists, the app falls back to a synthesized two-tone beep, so
everything still works; it just won't be your sound.
